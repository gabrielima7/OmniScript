name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master]

jobs:
  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: '.'
          severity: warning
          check_together: 'yes'
        env:
          SHELLCHECK_OPTS: -x

  syntax-check:
    name: Bash Syntax Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check bash syntax
        run: |
          find . -name "*.sh" -type f | while read -r script; do
            echo "Checking syntax: $script"
            bash -n "$script" || exit 1
          done

  test-help:
    name: Test CLI Help
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test version command
        run: ./omniscript.sh --version

      - name: Test help command
        run: ./omniscript.sh --help

  test-multi-distro:
    name: Test on ${{ matrix.distro }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - distro: ubuntu
            image: ubuntu:22.04
          - distro: debian
            image: debian:bookworm
          - distro: fedora
            image: fedora:39
          - distro: alpine
            image: alpine:3.19
          - distro: archlinux
            image: archlinux:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test in ${{ matrix.distro }}
        run: |
          docker run --rm -v "$PWD:/omniscript" -w /omniscript ${{ matrix.image }} bash -c "
            # Install bash if needed (Alpine)
            if command -v apk &>/dev/null; then
              apk add --no-cache bash curl
            fi
            
            # Test help
            ./omniscript.sh --help
            
            # Test version
            ./omniscript.sh --version
            
            echo '✅ Tests passed on ${{ matrix.distro }}'
          "

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [shellcheck, syntax-check, test-help]
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create tarball
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          tar -czf omniscript-${VERSION}.tar.gz \
            --exclude='.git' \
            --exclude='.github' \
            .

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: omniscript-*.tar.gz
          generate_release_notes: true
